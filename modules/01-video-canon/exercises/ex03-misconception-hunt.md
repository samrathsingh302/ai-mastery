# Exercise 03 — Misconception hunt (true/false, self-checking)

**Rules:** mark each statement TRUE or FALSE, from memory. Then EITHER run the self-checker
(`python check.py` in this folder — works on any Python 3, no installs; if Python isn't set up
yet, module 02 does that — use the answers file instead) OR check against
`solutions/ex03-answers.md`. Target: 10+/12. Every miss → re-read the section the checker names.

1. An LLM looks up answers in a database of stored documents.
2. A base model and an assistant model differ mainly in how much data they were trained on.
3. The model reads and writes tokens, not letters.
4. Hallucination means the model is deliberately deceiving you.
5. Information pasted into the conversation is more reliably used than information the model
   "remembers" from training.
6. Asking a model "what model are you?" is a reliable way to find out what it is.
7. Chain-of-thought reasoning improves answers because each emitted token buys the model more
   computation.
8. A model that solves PhD-level physics problems will also reliably compare 9.11 and 9.9.
9. RL with automatic answer-checking works best in domains like maths and code.
10. RLHF optimises the model to be factually correct.
11. Vibe coding is safe in proportion to your ability to read and debug the generated code.
12. "AI engineer" is primarily a machine-learning research role.
