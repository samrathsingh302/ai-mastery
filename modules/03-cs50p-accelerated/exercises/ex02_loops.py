"""Rung 2 — loops. Both of these are the accumulator pattern; write it from memory first."""


def total_volume(sets):
    """sets is a list of (weight, reps) pairs, e.g. [(100, 5), (110, 3)].
    Return the total volume: sum of weight*reps across all sets. Empty list -> 0."""
    raise NotImplementedError("your code here")


def count_vowels(text):
    """Return how many vowels (aeiou, case-insensitive) appear in text."""
    raise NotImplementedError("your code here")
