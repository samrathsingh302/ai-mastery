"""Mission 1 — paths. check.py builds a sandbox tree and passes you its root Path."""


def count_by_suffix(root):
    """Recursively count FILES by suffix under root. Return a dict like
    {".md": 2, ".py": 1}. Skip directories; keep the leading dot; lowercase the suffix."""
    raise NotImplementedError("your code here")


def biggest_file(root):
    """Return (filename, size_in_bytes) of the largest file anywhere under root.
    (p.name, not the full path.)"""
    raise NotImplementedError("your code here")
