# Wednesday 16/09/2026 — sheet 1 of 7

## Today

- **Date:** Wed 16/09/2026. **Shift:** none today — the whole day is yours.
- **Applications:** none today. The CV is being rebuilt today (ledger 357); Thursday opens the
  submission week with Lloyds.
- **Order (do it top to bottom):** 1. ex02 typed · 2. NeetCode #2 · 3. SWI-Prolog installed.
- One rule for today: **you type first.** ex02 has been explained five times (27/08 → 12/09).
  There is no sixth explanation on this sheet and you do not need one.

## Do

### 1. Write `ex02_loops.py` — both functions

- **File:** `C:\Users\samra\repos\ai-mastery\modules\03-cs50p-accelerated\exercises\ex02_loops.py`
- **Folder to open in the terminal:** `C:\Users\samra\repos\ai-mastery`
- **What:** replace the two `raise NotImplementedError` lines with your own code.
  `total_volume(sets)` sums `weight * reps` over a list of pairs; `count_vowels(text)` counts
  aeiou in a string, case-insensitive. Write from a blank head first, look at nothing.
- **The shape, on a different problem** (this is NOT either answer — it is the same skeleton
  wearing other clothes). Longest word in a list:

  ```python
  def longest(words):
      best = ""                 # 1. start the accumulator (the "nothing yet" value)
      for word in words:        # 2. visit each item, one at a time
          if len(word) > len(best):
              best = word       # 3. fold this item in
      return best               # 4. hand it back
  ```

  That is the whole skeleton; the rest is yours.
- **Check (run from `C:\Users\samra\repos\ai-mastery`):**

  ```
  python modules\03-cs50p-accelerated\exercises\check.py ex02
  ```
- **Done when:** the output says `ex02_loops` nowhere in a TODO or FAIL line and the summary
  reads `4 passed · 0 failed · 0 exercises not started`. TODO = not written; FAIL = written and
  wrong, and it prints expected vs got — read that line, it is the whole debugging lesson.
- Reference if you want the book version afterwards (read it AFTER you pass, not before):
  `C:\Users\samra\repos\ai-mastery\modules\03-cs50p-accelerated\TEACH.md`, "Rung 2 — Loops".

### 2. NeetCode #2 — the next problem in Arrays & Hashing

- **Where:** https://neetcode.io/practice → the **NeetCode 150** list → section **Arrays &
  Hashing** → the first problem you have not solved (you have #1, Contains Duplicate, done at
  `C:\Users\samra\repos\ai-mastery\neetcode\001_contains_duplicate.py`). The site's own order is
  the order — do not skip down the list.
- **Your file:** `C:\Users\samra\repos\ai-mastery\neetcode\002_<problem_name>.py` (creates) —
  same naming as 001: number, underscore, the problem's name in lower_snake_case.
- **How to work it:** 20 minutes on your own first, brute force allowed and encouraged; write
  your own `assert` lines at the bottom of the file (`if __name__ == "__main__":`) so the file
  proves itself. Only then read the editorial/video.
- **Check (run from `C:\Users\samra\repos\ai-mastery`):**

  ```
  python neetcode\002_<problem_name>.py
  ```
- **Done when:** the file runs silently (asserts pass), the problem is green on neetcode.io, and
  you can say out loud what the time complexity is and why.

### 3. Install SWI-Prolog (the KRR lane opens)

- **Where:** https://www.swi-prolog.org/download/stable → the Windows 64-bit self-installing
  `.exe` (stable line is 10.0.2-1 as of 16/09/2026). Accept the defaults, including "add to PATH".
- **Why now:** Knowledge Representation & Reasoning is the most alien Sem-1 module, so it starts
  first and its Anki cards start in W0 (UNI-PREP.md §1). Today is the install only — chapter 1 is
  tomorrow.
- **Check (a NEW PowerShell window, so it picks up the new PATH):**

  ```
  swipl --version
  ```
- **Done when:** it prints a version line (e.g. `SWI-Prolog version 10.x.x for x64-win64`). If
  the command is not found, the installer did not add it to PATH — reopen the terminal first,
  then re-run the installer and tick that box.

## If stuck

The tutor window is **on call**, not teaching. Open `/study-session` and paste **one** of these,
verbatim, with the real text:

- the line you are stuck on, plus what you expected it to do;
- the exact error or `FAIL` line the checker printed (all of it, including the expected-vs-got);
- a one-sentence question ("why does my total come out 0?").

It will not propose a plan, re-explain the topic, or hand you code. Give it 30 minutes of
genuine struggle first (the contract's rule 8) — the wall is the curriculum.

## Close

1. **Anki** — clear today's queue first if you have not already; then, if today produced a miss
   worth carding, write the cards yourself in the terminal (format law:
   `C:\Users\samra\repos\ai-mastery\anki\CARD-RULES.md`). The session is a typo-scribe only.
2. **Journal line** — append one line to `C:\Users\samra\repos\ai-mastery\journal.md`:
   date, what went green, the miss, struggle minutes, AI-free Y/N, misses-to-cards count.
3. **Applications** — nothing to record today. (For any day you do submit:
   `python career\apply.py status <id> submitted` from `C:\Users\samra\repos\ai-mastery`.)
4. **Carry-forward** — anything unfinished goes to the TOP of its lane on tomorrow's sheet,
   `C:\Users\samra\repos\ai-mastery\sheets\2026-09-17-thu.md`, named in the journal line. Never
   dropped silently.
